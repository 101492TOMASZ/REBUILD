[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: Tests requiring real model files (slow)
    slow: Tests that take a long time to execute

addopts = -v --tb=short -p no:warnings
